import { Board, KeyBoard, KeyCap, KeyRow, hiddenInput } from './base'
import { buttonContainer } from './containers'
import { largeText, letter, smallText } from './text'

const styles = {
  Board,
  buttonContainer,
  KeyBoard,
  KeyCap,
  KeyRow,
  largeText,
  letter,
  smallText,
  hiddenInput
}

export default styles